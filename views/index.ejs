<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title><%= title %></title>
    </head>
    <body>
        <ul>
            <% messages.map((message) => { %>
                <p>user: <%= message.user %></p>
                <p>says: <%= message.text %></p>
                <p>date: <%= message.added %></p>
            <% }) %>
        </ul>
    </body>
</html>
